services:
  grafana:
    ports:
      - 3000:3000
  kibana:
    ports:
      - 5601:5601
  filebeat:
    networks:
      - monitoring
  pghero:
    networks:
      - common_net


networks:
  common_net:
    name: ${COMMON_NET_NAME}
    external: true
